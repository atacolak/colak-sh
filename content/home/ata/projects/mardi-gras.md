blurb: operator work cockpit for beads. the tree stays a tree

# mardi-gras

origin: [github.com/atacolak/mardi-gras](https://github.com/atacolak/mardi-gras)
upstream: a beads parade aimed at `bd`. this fork is the cockpit we actually run.

`mg` reads the same issues agents write.
children live under their epic.
blocked and deferred rows stay with their parent.
closed epics tuck into a closed section.
the selected family's status glyphs stay full color; everyone else recedes.

six named states, not carnival buckets:

```text
● ready     ◐ working    ⊘ waiting/blocked
⏸ deferred  ○ review     ✓ done
```

`br` is first-class.
the mouse can reach the beads.
previews stack.
screenshots of the live cockpit are on the origin (`docs/screenshots/`), which is the right place for them.

honest limit: the actors pane is still a sketch.
this is a board, not the village.
actor village is the system; mardi-gras is how you watch the work.
